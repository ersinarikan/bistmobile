# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# Local Android SDK path
**/local.properties

# SonarScanner workdir
.scannerwork/

# Firebase native configs (local only — do not commit)
android/app/google-services.json
ios/Runner/GoogleService-Info.plist

# Push / FCM secrets for tool/ios_push_bootstrap.sh
.secrets/fcm.env
.secrets/*.p8
.secrets/AuthKey_*.p8
.secrets/firebase-adminsdk.json
.secrets/*-firebase-adminsdk-*.json
.secrets/*.json
!.secrets/fcm.env.example

# Local clone of web repo for agent reference
_ref_bist/

# Cursor rules/skills — local agent config only (do not publish)
.cursor/
