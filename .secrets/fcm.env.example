# Local only — copy to fcm.env and fill. Never commit fcm.env / *.json.
FCM_ENABLED=1
WATCHLIST_ALERTS_ENABLED=1
FCM_PROJECT_ID=lotlotnet-8c348
FCM_SERVICE_ACCOUNT_FILE=/absolute/path/to/firebase-adminsdk.json
# Optional legacy fallback (prefer HTTP v1 above):
# FCM_SERVER_KEY=
