# Google Play Billing — server env (commit etme; gerçek değerler .secrets/)
# After Play Console links the SA, run: tool/android_play_billing_bootstrap.sh install

GOOGLE_PLAY_PACKAGE_NAME=com.lotlot.lotlotnet_mobile
GOOGLE_PLAY_SERVICE_ACCOUNT_FILE=/opt/bist-pattern/.secrets/google-play-sa.json

# IAP_PRODUCT_TIERS_JSON already on server via iap-product-tiers.env (includes v2 + premium).
# platforms.google_play becomes true automatically when PACKAGE_NAME + SA file are set.
